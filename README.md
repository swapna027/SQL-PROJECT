# SQL-PROJECT
Here we can using the tool MYSQL workbench.
This project gives you practice with DDL, DML, SELECT, WHERE, LIKE, JOIN, GROUP BY, HAVING, aggregate functions, date functions, string functions, and CASE statements.
